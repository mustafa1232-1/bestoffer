name: maslaki_company
description: "Maslaki Company Operations application"
publish_to: "none"
version: 1.0.0+3

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  app_company_runtime:
    path: ../../packages/app_company_runtime

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_riverpod: ^2.5.1
  core_auth:
    path: ../../packages/core_auth
  core_storage:
    path: ../../packages/core_storage

flutter:
  uses-material-design: true
