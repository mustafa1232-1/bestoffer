# Docker compose variables
POSTGRES_DB=bestoffer
POSTGRES_USER=bestoffer
POSTGRES_PASSWORD=replace_with_strong_password
JWT_SECRET=replace_with_long_random_secret

# Optional (mainly for web clients)
CORS_ORIGINS=*
