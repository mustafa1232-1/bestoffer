name: app_delivery_runtime
description: Delivery app runtime bootstrap package for Maslaki split architecture.
publish_to: "none"
version: 1.0.0

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  core_auth:
    path: ../core_auth
  core_design_system:
    path: ../core_design_system
  core_localization:
    path: ../core_localization
  core_networking:
    path: ../core_networking
  core_storage:
    path: ../core_storage
  dio: ^5.7.0
