name: core_design_system
description: Shared design system primitives and theme tokens.
publish_to: "none"
version: 1.0.0

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.3.3
