name: core_networking
description: Shared networking clients and transport concerns.
publish_to: "none"
version: 1.0.0

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  dio: ^5.9.0
  core_storage:
    path: ../core_storage
